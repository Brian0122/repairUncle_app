<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @model.name %>
</p>

<p>
  <b>Make:</b>
  <%= Make.find(@model.make_id).name %>
</p>


<%= link_to 'Edit', edit_model_path(@model), :class => 'btn btn-success' %> 
<%= link_to 'Back', models_path, :class => 'btn btn-warning' %>
