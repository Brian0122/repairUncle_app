<h1>Listing workshops</h1>

<table class="table table-striped table-condensed">
  <tr>
    <th>Name</th>
    <!-- <th>Address</th>
    <th>Contact person</th>
    <th>Contact numbers</th>
    <th>Website</th>
    <th>Opening hours</th>
    <th>Is premium</th>
-->
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @workshops.each do |workshop| %>
  <tr>
    <td><%= workshop.name %></td>
    <!-- <td><%= workshop.address %></td>
    <td><%= workshop.contact_person %></td>
    <td><%= workshop.contact_numbers %></td>
    <td><%= workshop.website %></td>
    <td><%= workshop.opening_hours %></td>
    <td><%= workshop.is_premium %></td> -->
    <td><%= link_to 'Show', workshop , :class => 'btn btn-primary'%></td>
    <td><%= link_to 'Edit', edit_workshop_path(workshop) , :class => 'btn btn-success'%></td>
    <td><%= link_to 'Destroy', workshop, method: :delete, 
    data: { confirm: 'Are you sure?' } , :class => 'btn btn-danger'%></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Workshop', new_workshop_path , :class => 'btn btn-info' %>
